version: '3.7'
services:
  tweet_capture:
    command: /wrapped-driver-env/bin/python -m pytest -vv tests/test_tweet_capture.py
    image: balexander85/tweet_capture:new
    volumes:
      - ./screenshots:/usr/src/screenshots
