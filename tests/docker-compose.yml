version: '3.7'
services:
  tweet_capture:
    build: .
    command: /wrapped-driver-env/bin/python -m pytest -vv test_tweet_capture.py
    image: balexander85/tweetcapture_test
    volumes:
      - ./screenshots:/usr/src/screenshots
      - ${PWD}/tests/test_tweet_capture.py:/usr/src/test_tweet_capture.py
